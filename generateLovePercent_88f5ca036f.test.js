// Test generated by RoostGPT for test ExpressJs using AI Type Open AI and AI Model gpt-4

import { generateLovePercent } from './features';

describe('generateLovePercent', () => {
  test('should return a string', () => {
    const result = generateLovePercent();
    expect(typeof result).toBe('string');
  });

  test('should return a string ending with %', () => {
    const result = generateLovePercent();
    expect(result.slice(-1)).toBe('%');
  });

  test('should return a string whose number part is between 0 and 100', () => {
    const result = generateLovePercent();
    const numberPart = parseInt(result.slice(0, -1));
    expect(numberPart).toBeGreaterThanOrEqual(0);
    expect(numberPart).toBeLessThanOrEqual(100);
  });
});
