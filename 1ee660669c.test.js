// Test generated by RoostGPT for test ExpressJs using AI Type Open AI and AI Model gpt-4

import mongoose from 'mongoose';
import { MongoMemoryServer } from 'mongodb-memory-server';

const mongoServer = new MongoMemoryServer();

describe('Mongo Connection', () => {
  beforeAll(async () => {
    const uri = await mongoServer.getUri();
    await mongoose.connect(uri, {
      useNewUrlParser: true,
      useUnifiedTopology: true,
    });
  });

  test('should be connected to the database', async () => {
    const isConnected = mongoose.connection.readyState;
    expect(isConnected).toBe(1);
  });

  test('should not be connected to the wrong database', async () => {
    const uri = 'mongodb://127.0.0.1:27017/wrongDB';
    await mongoose.connect(uri, {
      useNewUrlParser: true,
      useUnifiedTopology: true,
    }).catch((e) => {
      expect(e).toBeTruthy();
    });
  });

  afterAll(async () => {
    await mongoose.disconnect();
    await mongoServer.stop();
  });
});
