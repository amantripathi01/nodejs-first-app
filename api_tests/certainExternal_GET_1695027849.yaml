# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_GET_1695027849.csv"
    fields:
      - "accountCode"
    skipHeader: true
  plugins:
    expect: {}
    
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}"
          auth:
            user: "{{ $processEnvironment.Username }}"
            pass: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "ProfileObj"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/invalidAccountCode"
          auth:
            user: "{{ $processEnvironment.Username }}"
            pass: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/1234567890"
          auth:
            user: "{{ $processEnvironment.Username }}"
            pass: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "ProfileObj"
