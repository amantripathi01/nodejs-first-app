# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "certainExternal_POST_1695028725.csv"
    fields:
      - "firstName"
      - "lastName"
      - "profilePin"
    skipHeader: true
  http:
    timeout: 10
  phases:
    - duration: 60
      arrivalRate: 20

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/:accountCode"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          headers:
            Content-Type: "application/json"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
            profilePin: "{{ profilePin }}"
          expect:
            - statusCode: 200
            - hasProperty: "result"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid/url"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          headers:
            Content-Type: "application/json"
          json:
            firstName: "{{ firstName }}"
            lastName: "{{ lastName }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/:accountCode"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          headers:
            Content-Type: "application/json"
          json:
            firstName: "SPECIAL_CHARACTERS"
            lastName: "LONG_STRING_12345678901234567890123456789012345678901234567890"
            profilePin: "SPECIAL_CHARACTERS"
          expect:
            - statusCode: 200
            - hasProperty: "result"
