# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=46a2c81c36

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "logout_get.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "required properties"
            - hasProperty: "nullable properties"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/user/logout"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "required properties"
            - hasProperty: "nullable properties"
