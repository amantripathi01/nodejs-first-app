# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=d01ae3c3ac

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "createWithArray_post.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/user/createWithArray"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          capture:
            json: "$"
            as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "responseBody.success"
            - equals: 
                - "{{ responseBody.success }}"
                - true

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalidURL"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - post:
          url: "/user/createWithArray"
          json:
            field1: "edge_case_value"
            field2: "edge_case_value"
          capture:
            json: "$"
            as: "responseBody"
          expect:
            - statusCode: 200
            - hasProperty: "responseBody.success"
            - equals: 
                - "{{ responseBody.success }}"
                - true
