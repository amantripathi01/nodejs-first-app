# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_GET_1695027726.csv"
    fields:
      - "accountCode"
      - "profilePin"
    skipHeader: true
  plugins:
    expect: {}
  defaults:
    headers:
      Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          expect:
            - statusCode: 200
            - hasProperty: "ProfileObj"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidPath/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Profile/!@#$$%^^/{{ accountCode }}/{{ profilePin }}"
          expect:
            - statusCode: 200
            - hasProperty: "ProfileObj"
