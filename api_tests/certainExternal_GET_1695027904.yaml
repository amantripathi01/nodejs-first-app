# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  environments:
    development:
      target: "https://dev.example.com"
    production:
      target: "https://prod.example.com"
  payload:
    path: "certainExternal_GET_1695027904.csv"
    fields:
      - "accountCode"
      - "eventCode"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{accountCode}}/{{eventCode}}"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "EventObj"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidURL"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/veryLongAccountCodeWithSpecialCharacters/veryLongEventCodeWithSpecialCharacters"
          auth:
            type: "basic"
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: "{{ response.statusCode }}"
            - hasProperty: "EventObj"
