# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "certainExternal_DELETE_1695027785.csv"
    fields:
      - "accountCode"
      - "profilePin"
  processor: "./my-processor.js"
  plugins:
    expect: {}

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "ProfileObj"

  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/invalid/url"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/certainExternal/service/v1/Profile/{{ accountCode }}/{{ profilePin }}"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "ProfileObj"
