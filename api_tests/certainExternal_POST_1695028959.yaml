# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "certainExternal_POST_1695028959.csv"
    fields:
      - "accountCode"
      - "eventCode"
    skipHeader: true
  environments:
    staging:
      target: "https://staging.example.com"
    production:
      target: "https://example.com"

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          json:
            field1: "valid value"
            field2: "valid value"
          expect:
            - statusCode: 200
            - hasProperty: "expectedPropertyName"
  - name: "API request with validation failure"
    flow:
      - post:
          url: "/INVALID_URL"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          json:
            field1: "invalid value"
            field2: "invalid value"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - post:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          headers:
            Authorization: "Basic {{ $processEnvironment.Username }}:{{ $processEnvironment.Password }}"
          json:
            field1: "extremely large value"
            field2: "value with special characters !@#$%^&*()"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "expectedPropertyName"
