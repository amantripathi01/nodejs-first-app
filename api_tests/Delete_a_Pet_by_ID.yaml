// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}/pet"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Delete a Pet by ID"
    flow:
      - log: "Scenario: Delete a Pet by ID"
      - delete:
          url: "/{{ petId }}"
          headers:
            Content-Type: "application/json"
      - think: 1
      - log: "When a user requests to delete a pet with ID {petId}"
      - think: 1
      - capture:
          json: "$.status"
          as: "status"
      - assert:
          - path: "status"
            equals: 200
      - think: 1
      - log: "Then the response status code should be 200"
