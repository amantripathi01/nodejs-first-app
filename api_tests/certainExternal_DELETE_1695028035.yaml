# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "certainExternal_DELETE_1695028035.csv"
    fields:
      - "accountCode"
      - "eventCode"
    skipHeader: true
  environments:
    variable:
      hostname: "{{ $processEnvironment.hostname }}"
  auth:
    type: "basic"
    basic:
      username: "{{ $processEnvironment.Username }}"
      password: "{{ $processEnvironment.Password }}"

scenarios:
  - name: "Successful API request"
    flow:
      - delete:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          auth: 
            type: "basic"
            basic:
              username: "{{ $processEnvironment.Username }}"
              password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "id"

  - name: "API request with validation failure"
    flow:
      - delete:
          url: "/invalid/url/{{ accountCode }}/{{ eventCode }}"
          auth: 
            type: "basic"
            basic:
              username: "{{ $processEnvironment.Username }}"
              password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400

  - name: "API request with edge case"
    flow:
      - delete:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}/{{ eventCode }}"
          auth: 
            type: "basic"
            basic:
              username: "{{ $processEnvironment.Username }}"
              password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: "{{ $processEnvironment.statusCode }}"
            - hasProperty: "message"
