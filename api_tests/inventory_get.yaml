# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=bc9f21855d

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/store/inventory"
          expect:
            - statusCode: 200
  - name: "API request with validation failure"
    flow:
      - get:
          url: "/store/invalid"
          expect:
            - statusCode: 400
  - name: "API request with edge case"
    flow:
      - get:
          url: "/store/inventory?data=@@@@@@@"
          expect:
            - statusCode: 200
