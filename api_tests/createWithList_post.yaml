# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

# RoostTestHash=e2a910fba9

config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "createWithList_post.csv"
    fields:
      - "field1"
      - "field2"
    skipHeader: true

scenarios:
  - name: "Successful API request"
    flow:
      - post:
          url: "/user/createWithList"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 200
            - hasProperty: "required_properties"
            - hasProperty: "nullable_properties"

  - name: "API request with validation failure"
    flow:
      - post:
          url: "/invalid_url"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "{{ field1 }}"
            field2: "{{ field2 }}"
          expect:
            - statusCode: 400
            - hasProperty: "error"

  - name: "API request with edge case"
    flow:
      - post:
          url: "/user/createWithList"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          json:
            field1: "special_characters_@#$%^&*"
            field2: "large_data_1234567890"
          expect:
            - statusCode: "{{ $between(200, 500) }}"
            - hasProperty: "required_properties"
            - hasProperty: "nullable_properties"
