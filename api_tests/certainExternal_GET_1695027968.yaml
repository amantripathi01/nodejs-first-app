# Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "certainExternal_GET_1695027968.csv"
    fields: ["accountCode"]
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  environments:
    production:
      target: "https://prod.example.com"
    staging:
      target: "https://staging.example.com"

scenarios:
  - name: "Successful API request"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/{{ accountCode }}"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 200
            - hasProperty: "events"
            - hasHeader: "content-type"

  - name: "API request with validation failure"
    flow:
      - get:
          url: "/invalidURL"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 404

  - name: "API request with edge case"
    flow:
      - get:
          url: "/certainExternal/service/v1/Event/!@#$%^&*()"
          auth:
            username: "{{ $processEnvironment.Username }}"
            password: "{{ $processEnvironment.Password }}"
          expect:
            - statusCode: 400
