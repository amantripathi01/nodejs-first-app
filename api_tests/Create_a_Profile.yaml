// Test generated by RoostGPT for test go-test using AI Type Open AI and AI Model gpt-4

config:
  target: "https://example.com"
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - name: "Create a Profile"
    flow:
      - post:
          url: "/certainExternal/service/v1/Profile/:accountCode"
          headers:
            Header Name: Header Value
          json:
            firstName: "sd47FYNR-T2WL"
            lastName: "Jsdfames8WY"
            profilePin: "aP4AGF05I0sdfOJDBX8"
          capture:
            - json: "$.profileId"
              as: "profileId"
            - json: "$.uuid"
              as: "uuid"
            - json: "$.dateCreated"
              as: "dateCreated"
            - json: "$.dateModified"
              as: "dateModified"
            - json: "$.externalKey"
              as: "externalKey"
            - json: "$.isActive"
              as: "isActive"
            - json: "$.prefix"
              as: "prefix"
            - json: "$.suffix"
              as: "suffix"
            - json: "$.firstName"
              as: "firstName"
            - json: "$.lastName"
              as: "lastName"
            - json: "$.badgeName"
              as: "badgeName"
            - json: "$.dob"
              as: "dob"
            - json: "$.position"
              as: "position"
            - json: "$.organization"
              as: "organization"
            - json: "$.phone"
              as: "phone"
            - json: "$.phone2"
              as: "phone2"
            - json: "$.phoneMobile"
              as: "phoneMobile"
            - json: "$.fax"
              as: "fax"
            - json: "$.email"
              as: "email"
            - json: "$.ccAssistantOnEmails"
              as: "ccAssistantOnEmails"
            - json: "$.ccAssociateOnEmails"
              as: "ccAssociateOnEmails"
            - json: "$.accountCode"
              as: "accountCode"
            - json: "$.profilePin"
              as: "profilePin"
            - json: "$.pkProfileId"
              as: "pkProfileId"
            - json: "$.showOnline"
              as: "showOnline"
            - json: "$.imported"
              as: "imported"
            - json: "$.source"
              as: "source"
            - json: "$.password"
              as: "password"
          expect:
            - statusCode: 200
            - hasProperty: "profileId"
            - hasProperty: "uuid"
            - hasProperty: "dateCreated"
            - hasProperty: "dateModified"
            - hasProperty: "externalKey"
            - hasProperty: "isActive"
            - hasProperty: "prefix"
            - hasProperty: "suffix"
            - hasProperty: "firstName"
            - hasProperty: "lastName"
            - hasProperty: "badgeName"
            - hasProperty: "dob"
            - hasProperty: "position"
            - hasProperty: "organization"
            - hasProperty: "phone"
            - hasProperty: "phone2"
            - hasProperty: "phoneMobile"
            - hasProperty: "fax"
            - hasProperty: "email"
            - hasProperty: "ccAssistantOnEmails"
            - hasProperty: "ccAssociateOnEmails"
            - hasProperty: "accountCode"
            - hasProperty: "profilePin"
            - hasProperty: "pkProfileId"
            - hasProperty: "showOnline"
            - hasProperty: "imported"
            - hasProperty: "source"
            - hasProperty: "password"
